//Tipos Dados Primitivos:

let nome = 'Janaina'; //String: letras, números, caracteres especiais
let nome1 = "Janaina"; //String: letras, números, caracteres especiais
let nome2 = `Janaina`; //String: letras, números, caracteres especiais
let idade = 38; // number literal
let nota = 9.5 // number (ponto flutuante)
let estaAprovado = true; //boolean true ou false (lógico)
let sobrenome = undefined; // Undefined ->(o js que seta essa classificação) não aponta pra local nenhum na memória
let corSelecionado = null //(Nós definimos) para indicar que a variavel não aponta pra nenhum local na memória.
// typeof saber o tipo de dado
console.log(typeof idade)